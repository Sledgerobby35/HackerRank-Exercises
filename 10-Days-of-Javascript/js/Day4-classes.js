(function(){
	"use strict"
	class Polygon {
		constructor(height, width) {
			this.height = height;
			this.width = width;
		}
	}
	let arr = [1,2,3,4]
	let c = new Polygon(arr);
	console.log(c);
})();