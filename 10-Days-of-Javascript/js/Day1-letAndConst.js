(function(){
	"use strict";
	function main(){
		const PI = Math.PI;
		let r = readLine().;
		if(0 < r < 100){
			console.log(PI * (Math.pow(r, 2)));
			console.log(2 * (PI * r));
		} else {
			console.log(false);
		}
	}
})();